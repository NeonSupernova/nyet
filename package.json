{
  "name": "nyet",
  "private": true,
  "description": "Repo-level tooling (git hooks) for the Nyet compiler. The compiler itself is pure-stdlib Python under pynyet/ — see CLAUDE.md.",
  "scripts": {
    "prepare": "husky"
  },
  "devDependencies": {
    "@commitlint/cli": "^21.2.1",
    "@commitlint/config-conventional": "^21.2.0",
    "husky": "^9.1.7"
  }
}
