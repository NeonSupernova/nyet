;; Should be rejected: `let q p` consumes p, then `(out p)` is a UAF.

(struct Person name:string age:i32)

(fn use_one (p:Person) -> unit
  (out! (. p name)))

(fn main () -> unit
  (let p (Person name:"alice" age:30))
  (use_one p)
  (use_one p))
