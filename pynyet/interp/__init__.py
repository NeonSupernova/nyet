"""Tree-walk interpreter.

Placeholder for the tree-walk interpreter used for fast iteration and
feature validation during development, per PLAN.md §1 (project layout).

Planned modules:
    interp.py — tree-walking evaluator
    value.py  — runtime value representation

None of these modules exist yet. This package exists so
`import pynyet.interp` succeeds.
"""
