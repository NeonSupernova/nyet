"""Mid-level IR and lowering passes.

Placeholder for the v0.4 IR pipeline per PLAN.md §6:

    ir.py        — typed, desugared, SSA-ish mid-level IR
    lower.py     — AST → IR lowering
    monomorph.py — generic specialization, name mangling

None of these modules exist yet. This package exists so
`import pynyet.ir` succeeds.
"""
